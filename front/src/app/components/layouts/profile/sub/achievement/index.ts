import * as React from 'react';
import * as cn from 'classnames';

interface IProps {
    className?: string;
    title?: string;
    children: any;
}

export class Achievement extends React.Component<IProps, any> {
    public render() {
        return <div />;
    }
}
